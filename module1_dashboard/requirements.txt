# Module 1 extra dependencies (on top of root requirements.txt's pydantic)
fastapi==0.137.0
uvicorn==0.49.0

# Optional: if ANTHROPIC_API_KEY is set, llm_summary.py uses this package
# to call a real LLM. Not required to run -- falls back to a rule-based
# summary when it's missing or unset.
# anthropic
