# Module 1 extra dependencies (on top of root requirements.txt's pydantic)
fastapi==0.137.0
uvicorn==0.49.0

# LLM summaries call a local Ollama server (default model qwen2.5:1.5b) via
# stdlib urllib -- no extra package needed. Falls back to a rule-based
# summary if Ollama is unreachable or not running.
